# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Build outputs
out/
# "target/" (workspace root), not "src-tauri/target/" — src-tauri became a
# Cargo workspace member when relay-server/ was added (see /Cargo.toml),
# and Cargo builds every member into one shared target dir at the root.
target/
src-tauri/gen/
src-tauri/icons/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

/out/